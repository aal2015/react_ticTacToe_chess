import {
    isCheckMate
} from './moveValidCheck';


const ChessPiecePoints = {
    "queen": 9,
    "rook": 5,
    "bishop": 3,
    "knight": 3,
    "pawn": 3,
};

class ChessMinMaxAlgo {
    constructor() {}

    minMax(board, pieceColor, enPassantState, castleState, moveCount) {
        
    }

    // isGameEnded(board, pieceColor, enPassantState, moveCount) {
        
    // }

    // isCheckMate(
    //                         boardClone,
    //                         enemyColor,
    //                         nextEnPassantState,
    //                         moveCount
    //                     );
}